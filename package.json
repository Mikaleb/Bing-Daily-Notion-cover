{
  "name": "bing-daily-notion-cover",
  "version": "1.0.0",
  "description": "Sync Bing daily picture to Notion cover",
  "main": "index.js",
  "engines": {
    "node": ">=16.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "release": "dotenv release-it -- && git add . && git commit -m 'chore(changelog): log' && git push"
  },
  "author": "Michaël Lebeau",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@notionhq/client": "^2.0.0",
    "auto-changelog": "^2.4.0",
    "axios": "^1.6.8",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "cz-conventional-changelog": "3.3.0",
    "dotenv-cli": "^6.0.0",
    "release-it": "^15.1.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
